# Task Completion — Definition of Done

> Part of this file is auto-generated from scripts detected in `package.json`.
> Review before use.

## Required Verification Commands

- [ ] TODO: no lint script detected in package.json — add the script or note the reason for skipping
- [ ] TODO: no typecheck script detected in package.json — add the script or note the reason for skipping
- [ ] TODO: no test script detected in package.json — add the script or note the reason for skipping
- [ ] `npm run build` — must pass before opening the PR

## Documentation Update Rules

TODO: project-specific documentation update rules (e.g. "new endpoint → update
api-contract.md") — this is a team decision, it cannot be detected automatically.

## PR Checklist

- [ ] All Verification Commands above PASS
- [ ] `verify-report.md` in `.caf/tasks/{TICKET-ID}/` shows Status: SUCCESS
- [ ] No changes outside the ticket scope
- [ ] TODO: additional project-specific checklist items (security review, migration check, etc.)

## Infrastructure Gap Notes

- No `lint` script detected in package.json (whole repo (root)). This is an infrastructure gap — a decision is needed: add the script, or deliberately skip this gate with the reason noted here.
- No `typecheck` script detected in package.json (whole repo (root)). This is an infrastructure gap — a decision is needed: add the script, or deliberately skip this gate with the reason noted here.
- No `test` script detected in package.json (whole repo (root)). This is an infrastructure gap — a decision is needed: add the script, or deliberately skip this gate with the reason noted here.
