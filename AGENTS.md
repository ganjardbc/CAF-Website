# AGENTS.md

> **DRAFT — must be reviewed.** This file was auto-generated by `caf-initiator`.
> The rules below are still placeholders — fill them in with concrete rules for this project,
> complete with right/wrong examples, before any agent uses them.

These instructions apply to all AI coding agents (Claude Code, Cursor, etc.), not just one tool.

## Workflow

Follow PIV: **Plan** first (write the plan, don't touch code) → **Implement** per the plan →
**Verify** (lint, typecheck, test) before declaring done. If verify fails, fix it and
retry (max 3x); if it still fails, stop and escalate to a human.

## Concrete Rules

TODO: fill in this project's specific rules. Example format:

### Rule 1: [rule name]
- **Right:**
  ```
  TODO: example code that follows the rule
  ```
- **Wrong:**
  ```
  TODO: example code that violates the rule
  ```

## Tracker

This project's ticket tracker: **GitHub Issues**. Artifact folder names
follow the ticket key (`ENG-123`, `#42`, etc.), consistent with the branch name
`ai-agent/{{TICKET-ID}}`.

## Constraints

- Don't auto-merge. Human review is required before merge at every phase.
- Don't auto-generate the contents of `.caf/knowledge/golden-examples/` or `.caf/knowledge/decisions/` —
  that's a human decision.
- Don't assume a verification script exists without checking `package.json` first.
